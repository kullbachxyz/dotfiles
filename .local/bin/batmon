#!/bin/sh
# 
while true
do
  battery_percentage=$(acpi -V | awk -F "," '{print $2}' | awk 'NR==1' | sed 's/[^0-9]//g')
  if [ $battery_percentage -lt 65 ]
  then
    notify-send -u critical "🪫Low Battery" "$battery_percentage% left"
  fi
  sleep 180
done
